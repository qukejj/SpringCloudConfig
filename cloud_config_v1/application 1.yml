server:
  port: 8761
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false  #表明是服务端，不是客户端
    fetch-registry: false  #表明不是客户端，不需要提取注册表
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
spring:
  application:
    name: 我是假设的